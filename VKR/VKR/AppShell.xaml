<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="VKR.AppShell"
			 x:Name="myshell"
	   xmlns:views="clr-namespace:VKR.Views">
	<!--сделать в коде с инициализацией для разных классов-->
    <!--
    
    <Shell.FlyoutHeader>
        <StackLayout BackgroundColor="{StaticResource Primary}" Padding="10">
            <Image HeightRequest="100" Source="xamarin.png" />
            <Label Text="Header" />
            <Label Text="Меню надзирателя"/>
        </StackLayout>
    </Shell.FlyoutHeader>
    
    -->

    <!--сделать в коде с инициализацией для разных классов-->
	<!--
	<FlyoutItem Title="Logout" Icon="xamarin.png" Route="Logout">
		<ShellContent ContentTemplate="{DataTemplate pages:Page2}"/>
	</FlyoutItem>
	
		TabBar lets you define content that won't show up in a flyout menu. When this content is active
		the flyout menu won't be available. This is useful for creating areas of the application where 
		you don't want users to be able to navigate away from. If you would like to navigate to this 
		content you can do so by calling 
		await Shell.Current.GoToAsync("//LoginPage");
	-->

</Shell>